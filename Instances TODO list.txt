Convert visitor data to savefile tags

Make visitor data per-instance instead of per-owner

Convert ownership to savefile tags

Convert saving to top-level instances directory
instances/Saved/instanceType/instance
instances/Temp/instanceType/instance

Add spawn point to instance-specific data





Low priority

Get the client F3 to show the biome correctly



Notes:
* Instances are not fully synced to clients; only dimensiontypes are synced
